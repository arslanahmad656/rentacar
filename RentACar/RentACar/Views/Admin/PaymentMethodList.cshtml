@model IEnumerable<RentACar.Models.PaymentMethod>

@{
    ViewBag.Title = "PaymentMethodList";
}

<h2>PaymentMethodList</h2>

<p>
    @Html.ActionLink("Create New", "CreatePaymentMethod")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |*@
            @Html.ActionLink("Delete", "DeletePaymentMethod", new { id=item.Id })
        </td>
    </tr>
}

</table>
